version: '3.8'

services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
    volumes:
      - ./media:/app/media
      - ./.env:/app/D:/youtube_news/.env # Maps local dotenv file inside context
    restart: always
